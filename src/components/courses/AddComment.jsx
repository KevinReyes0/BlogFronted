import {
    Button,
    Field,
    Fieldset,
    For,
    Input,
    NativeSelect,
    Stack,
  } from "@chakra-ui/react"

import { useState } from 'react';
import { Input } from '../settings/Input';
import { useCommentForm } from '../../shared/hooks/comments';

export const AddComment = () => {
    
}